const express = require('express');
const router = express.Router();
const { computeData } = require('../models/models.js'); // Importation du modèle

// Définir les routes
router.post('/compute', () => "zwzmplz");

module.exports = router;